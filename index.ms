# I so imports
import math;
import time;

# Under Variables
a = 0
const b = math.sqrt(2)

# if
if [a <= 2] {
    a = math.cos(1.5)
}

# Delecations
if [a > 2] {
    a = math.sqrt(4)
} else ({
    a = math.sqrt(2)
})